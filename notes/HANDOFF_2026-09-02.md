# Handoff — 2026-09-02 (paper-v2 integration finished)

Repository: `~/Documents/Research/pitch_fourier`. Supersedes `notes/HANDOFF_2026-08-30.md`.

## What happened between the two handoffs

- 2026-08-30 21:52 → 09-01 16:26: a GPT-5.6 Sol codex session (id `01a05439-cde4-7301-8cac-f2212659e8af`) took over
  the v4 correction pass. It rejected the first v4 preview run (global scaling, class-mean centroid still present,
  `hash()` seeds), froze a corrected feature contract, rewrote `phase5/theory_features.py`, `fingerprint.py`,
  `ckpt_fingerprint.py`, `ckpt_twins.py`, `thin_wikipedia.py`, added `crosscorpus_compare_v4.py`,
  `compare_v3_v4.py`, `validate_v4.py`, `v4_provenance.py`, `tests/test_phase5_v4.py` (19 tests), obtained
  independent verifier/reviewer READY on commit `a56ad3e`, ran the full chain (`phase5/rerun_v4.sh`,
  08-31 19:23 → 09-01 01:22 UTC, CPU only), and rewrote most of `paper/main.tex` from the v4 artifacts. The session
  ended mid-integration (last edit 09-01 16:26, uncommitted).
- 2026-09-02: this session verified every Phase-V number in the draft against the v4 JSONs, finished the remaining
  manuscript items (Fig 4/5/C.1 captions, spelled-view sentence in Result 5, Result 4 spacing, Table C.5 rows,
  Fig 7 tick labels, two small stale values), wrote `V4_CORRECTION_REPORT.md`, updated `PHASE5_RESULTS.md` (v4
  verdict + §10), `PHASE5_LOG.md`, `MANUSCRIPT_AUDIT.md` (round 4), `README.md`, inspected all 24 pages,
  then restructured the paper at the user's request (operator dependence + aliasing lead; Phase V compressed into §8
  with §8.1 held-out / §8.2 other corpora / §8.3 checkpoints; trajectory figure → Figure C.2; Results renumbered
  5/6 synthetic, 7/8/9 Phase V), committed, tagged `paper-v2`, rebuilt and pushed the mirror.
- `models/` (107 GB of weights) was deleted at the user's request on 2026-09-02. Nothing in the v4 chain, the
  paper build or the review reads it (the v4 provenance manifest lists no `models/` input). `data/` (31 GB) is
  still present.

## State

- Working repo: see `git log -1`; tags `paper-v1` = `fb3e3e8` (immutable), `paper-v2` = the commit after `a56ad3e`.
- Mirror: `github.com/VykosMolt/One-Concept-Multiple-Geometries`, rebuilt by `scripts/build_mirror.sh`, tag
  `paper-v2` pushed; `paper-v1` tag untouched.
- PDF: `paper/main.pdf`, 24 pages. Compile: `cd paper && ~/.local/bin/tectonic -X compile main.tex`; figures:
  `.venv-cpu/bin/python paper/make_figs.py` from the repo root (reads v4 files only).
- Tests: `.venv-cpu/bin/python -c` runner over `tests/test_phase5_v4.py` (no pytest installed; 19/19 pass).
  `python -m phase5.validate_v4 results/phase5/fingerprint/*_v4*.json` → all VALID.
  `python -m phase5.v4_provenance --verify=results/phase5/v4_provenance_manifest.json` fails closed on a dirty
  tree by design; the chain log records VALID at completion.
- No background jobs.

## Next

1. Fresh adversarial review of paper-v2 (Sol via `codex exec -m gpt-5.6-sol --sandbox read-only`, and/or an Opus
   review), briefed on the corrected claims; do NOT resume the old Sol review session (it was spent on the B1 draft).
2. Integrate findings; if the central number moves again, follow the standing rule (weaken the paper, do not search
   for a replacement metric).
3. Optional: NEXT_STEPS.md is pre-v4 and mostly about scale curves and mechanisms; the v4 outcome argues for a
   larger behavioural sample (more templates/families or more models) before any further Phase-V claim.
