# Handoff — 2026-08-30, 14:20 CEST (session paused on "Hold")

Repository: `~/Documents/Research/pitch_fourier`. Last commit `fb3e3e8`, tag `paper-v1` (working tree has
uncommitted changes, listed in §4). Public mirror: `github.com/VykosMolt/One-Concept-Multiple-Geometries`
(80 MB, tag `paper-v1`, built from `fb3e3e8` by `scripts/build_mirror.sh`; local copy
`~/Documents/Research/One-Concept-Multiple-Geometries`).

## 1. What was completed this session (all committed in fb3e3e8, pushed to the mirror)

- Title block: ORCID `0009-0005-2681-9027` and the mirror link, in the pattern of
  `Operational-Proto-Introspection/latex-v3/paper1.tex`; Reproducibility names the mirror, tag and
  `MIRROR_MANIFEST.md`.
- All 24 pages inspected as PNGs. Fixed: Fig 2(c) null label over bars; Fig 5(c) star vs title; Fig 6
  y-labels overlapping panels + "oracle" label; Fig 7(a) legend over data; Fig C.1 tick-label collisions and
  bar over axis top; URL/filename line breaks; `+0.037/ + 0.029` math spacing (47 spots); roadmap now lists
  Appendix D. Content errors caught: §2.3 and §7 still said "Poisson bootstrap"/"relabeling null (300)", glance
  box still quoted the withdrawn Poisson ± — replaced with shard-cluster CIs / B = 5,000.
- `scripts/build_mirror.sh` (rsync + SHA-256 manifest of excluded files), README "Public mirror" section,
  memory file updated.

## 2. Reviews (round 4, brief: `notes/review_brief_v4.md`)

**Opus 5** — done, `notes/review_opus5_v4.md`. Verdict MAJOR REVISION. 120 numbers checked, 20 mismatches.
I verified the important ones myself:

- **B1 confirmed (real bug).** `phase5/fingerprint.py` (feats, both views) and `phase5/ckpt_fingerprint.py`
  compute `circ` from `pc = (7*S) % 12` = semitone pitch class, i.e. the **chromatic** cyclic distance, not the
  circle-of-fifths distance `circ12(S_i, S_j)`. Spearman between the two over the 165 target-aggregated pairs
  is −0.19. So every held-out result in §7, §8, §10 controlled for the chromatic circle, not the fifths circle,
  while the abstract/central claim/contributions/discussion say "beyond explicit circle … baselines".
  `operators.py` and `corpus_cluster_boot.py` are correct (they use `keys15.candidate_geometries`).
- **B2 confirmed (magnitude).** The target-aggregated "line distance to the class mean" puts the merged twin
  classes at S = −1, 0, +1. With line/signed distance to the *nearest* spelling added, the gain shrinks a lot
  (my preview below).
- **B3 confirmed:** L209 template-robustness clause wrong on all three numbers (files: window +0.0145…+0.0465,
  document +0.0203…+0.0519, max p = .0040). Table C.7 itself is right.
- Also verified: "14 of 16" → 15 of 16 (only 7B×B enharmonic is 3/4); matched-operator "all p ≤ .001" → max
  .0016; Poisson SDs in Result 1 are the merged family's (±0.02/±0.03) spliced onto the canonical P = 0.87
  (canonical SDs were ±0.05/±0.05); max-over-layers p for the three n.s. models is 0.09–0.21 (not 0.25);
  black-key RSA standard-spelling floor is +0.56 (7B L2), not +0.70; "most of the rest slash notation" is
  false (3 of 21 non-"enharmonic" seam co-mentions have a slash); 7B "+12 top-1 in 8–9 of 15 keys" is really
  12 of 60 source×template cells (total scorer) / 15 of 60 (length-normalized), 5 of the 6 twin-bearing
  sources; Gemma's band-averaged context effect is −0.026 (so "+0.04…+0.07" must say three models);
  spelled/semantic subspace shares at the last position go to 0.76/0.73; 15-key merged-*scorer* line|circle is
  −0.23…−0.01 in every model (results/phase2/behavior/summary.txt "merged" rows) — the paper only reports the
  favourable circle|line half; corpus fifths partial had not converged with corpus size
  (results/corpus/wiki/convergence.json: +0.27 at 0.11 B words → +0.63 at 3.1 B, no plateau).
  Unverified but plausible (check before editing): #35/#36 (7B prompt-final p lower bound .004; Qwen
  last-token line n.s. under the glyph-preserving null), #69 (7B×doc CI lower bound rounds to +0.007),
  #80 (ΔCV range in the audit note is the spelled-view range).

**GPT-5.6 Sol via codex** — NOT delivered. `codex exec -m gpt-5.6-sol --sandbox read-only` ran ~40 min
(552k tokens) and then hit the ChatGPT usage limit ("try again at 6:27 PM"). No report was written. Session id
`01a0526c-3b9f-7551-abc8-29188be65805`; log at the scratchpad `codex_review.log`. To resume after 18:27 CEST:
`cd ~/Documents/Research/pitch_fourier && codex exec resume 01a0526c-3b9f-7551-abc8-29188be65805 -o notes/review_codex_sol_v4.md "Continue and deliver the full report now."`
(or start fresh with the same brief once the paper is revised — arguably better).

## 3. ⚠ A background recompute is RUNNING (launched 14:10, before the "Hold")

`phase5/rerun_v4.sh` (nohup, PID 1184626 at the time of writing; children are `python -m phase5.fingerprint --jobs=20`).
Log: `results/phase5/rerun_v4.log`; it ends with `RERUN_V4_DONE`. Expected ≈2.5–3 h total (Wikipedia
B = 5,000 runs ≈ 2–3 min each; cross-corpus 48 runs at B = 2,000; then `ckpt_fingerprint`).
It only reads existing inputs and writes NEW files (`results/phase5/fingerprint/*_v4*.json`,
`results/phase5/cond_wikipedia_thin_<corpus>_s{1..4}.npz`, `results/phase5/ckpt_fingerprint_v4.json`,
`results/phase5/ckpt_trajectory_v4.txt`); all v3 files are untouched. Nothing else was launched.
To stop it: `pkill -f phase5/rerun_v4.sh; pkill -f phase5.fingerprint`.

What it computes (the "v4" theory features, in the uncommitted `phase5/fingerprint.py` / `ckpt_fingerprint.py`):
true circle-of-fifths distance, chromatic distance (kept as a separate feature), line and signed distance to
the nearest spelling AND to the class mean (target-aggregated view), z-scored corpus column (D.6 said
"standardized"; v3 code did not standardize it); rich model adds cos(2π·chrom/12). Chain: Wikipedia both
views base/rich/target-prior (B = 5,000); docboot (B = 300); matched sym/rev/pmi; templates t0–3, lo0–3;
cross-corpus 4 corpora × 2 views + seed-0 thinned baselines + 4 extra thinning seeds per corpus (M5);
checkpoint trajectory. `PYTHONHASHSEED=0` pinned (the per-cell RNG seed uses `hash()` — v3 runs were not
seed-reproducible across processes).

**Preview under the v4 features (200 relabelings, modulation family, target-aggregated view; ΔKL nats/row,
theory KL₀ in brackets), compared with the published v3 numbers:**

| cell | v3 (paper) | v4 base | v4 + target prior | v4 rich |
|---|---|---|---|---|
| 1B × window | +0.037 | +0.016 (p .005) [0.113] | +0.019 | +0.015 |
| Gemma × window | +0.029 | +0.011 (p .005) [0.108] | +0.013 | +0.012 |
| Qwen × window | +0.016 | **+0.0025 (p .07, n.s.)** [0.095] | +0.006 (p .005) | +0.002 (n.s.) |
| 7B × window | +0.036 | +0.009 (p .005) [0.097] | +0.017 | +0.008 (p .01) |
| 1B × document | +0.045 | +0.025 [0.113] | +0.027 | +0.028 |
| Gemma × document | +0.036 | +0.019 | +0.022 | +0.020 |
| Qwen × document | +0.023 | +0.008 (p .005) | +0.013 | +0.008 |
| 7B × document | +0.039 | +0.013 | +0.022 | +0.015 |

So "14–34 %" becomes roughly 3–14 % (window) and 8–22 % (document); the document conditional survives in all
four models, the window conditional in three (Qwen n.s. at base and rich, significant with the target prior).
Direction and replication survive; the magnitude and one window cell do not. Per the standing rule
("if a targeted check weakens the story, change the story") the paper's Result 5, abstract, glance box,
central claim, contributions, discussion, Tables C.2/C.7 and Figs 5/7/C.1 must be re-stated from the v4 files
once the chain finishes — none of that has been done yet.

## 4. Uncommitted working tree

- `M phase5/fingerprint.py`, `M phase5/ckpt_fingerprint.py` — v4 features (see §3); ckpt writes
  `ckpt_fingerprint_v4.json`.
- `?? phase5/rerun_v4.sh`, `?? results/phase5/rerun_v4.log` (+ the v4 outputs as they appear).
- `?? notes/review_opus5_v4.md` (Opus review), `?? notes/HANDOFF_2026-08-30.md` (this file).
- Nothing committed since fb3e3e8; the mirror still reflects fb3e3e8 (i.e. the paper with the B1 bug).

## 5. Remaining work, in order

1. Decide whether the v4 feature set is the accepted baseline (it is strictly richer than both the v3 model and
   the reviewer's alternative). If yes, let the chain finish, then extend `phase5/crosscorpus_compare.py` so the
   size-matched baseline averages the per-row ΔKL over the five thinning seeds (`wikipedia_thin_<c>_v4`,
   `wikipedia_thin_<c>_s{1..4}_v4`) and rerun it for `_neutral` and spelled with the `*_v4` names; regenerate
   `results/phase5/scorer_robustness.txt`-style summaries only if they read fingerprint files (they don't).
2. Re-state every held-out number in `paper/main.tex` from the `*_v4` JSONs: L68 abstract, L89 glance, L118
   central claim, L120 contributions, Result 5 (L219), audit note (L225), template clause (L209), Result 6 (§8),
   Result 9 / Table C.5 (§10), Tables C.2, C.3, C.7, Fig 5/7/C.1 (`paper/make_figs.py` reads
   `wikipedia_v3_neutral*.json`, `ckpt_fingerprint.json`, `<corpus>_neutral.json` → point them at the v4 files),
   D.6 (feature list; "standardized" now true; λ = 1 is effectively OLS at n ≈ 154 — say so; §10's nuisance
   model has 15 source-row dummies — say so), `MANUSCRIPT_AUDIT.md`, `PHASE5_RESULTS.md`, `PHASE5_LOG.md`
   (chronological entry: review-driven, post-hoc feature correction, dated 2026-08-30), Appendix A rows for
   "circle feature was chromatic", "class-mean line encoding".
3. Text corrections that do not depend on the chain (evidence verified, see §2): L85/L87/L177 "14 of 16" →
   15 of 16; L150 Poisson SDs (canonical ±0.05/±0.05); L153 slash-notation sentence and merged-scorer line
   partials (report −0.23…−0.01, mostly n.s.; note it is near-definitional); L165 "+0.70" → "+0.56"; L172
   p = 0.09–0.21, subspace shares 0.53–0.76 / 0.46–0.73, "three models" for the context effect, 7B p lower
   bound .004, state which null for the Qwen/7B last-token line; L177 "at most 0.10" → 0.11; L180 top-1
   statistic; L196 "all p ≤ .002", "half to four fifths"; L228 uses the stale
   `fingerprint_wikipedia_v3.txt` (the only place the twin numbers exist — note it or regenerate); L261 drop
   "which is why §7 could treat the line as data structure"; Result 9 "circle|line ≈ 0 throughout" → mention the
   −0.25 excursion at 49B; §10 provenance numbers have no results file (`phase5/soup_check.py` prints only —
   save its output); Fig 4 caption: add the layers (7B L18 prompt-final; 7B L24 / Qwen L27 span-mean) and that
   they carry no max-over-layers null; Fig C.1 tick labels give window pair masses on the document panel;
   §3: add the corpus-size non-convergence caveat; §2.3/M4: state the control set per section (Result 1 uses
   block+commonness+other geometry; Result 4 and behaviour use glyph/edit/letter/alphabet/commonness);
   `sadek2026circle` venue; abstract "b = 0 … in the flagship cells" → six of eight (will change with v4 anyway).
4. Rebuild (`~/.local/bin/tectonic -X compile main.tex` in `paper/`; figures via
   `.venv-cpu/bin/python paper/make_figs.py` from the repo root), re-inspect every page, commit, move the tag
   (`git tag -f paper-v1` or create `paper-v2`), rebuild the mirror (`scripts/build_mirror.sh
   ~/Documents/Research/One-Concept-Multiple-Geometries`, then commit+push there, push the tag), and fix the
   Reproducibility sentence if the tag name changes.
5. Rerun the Sol review (after 18:27 CEST) on the revised draft; send a fresh Opus review if the rewrite is large.

## 6. Housekeeping facts

- Compile: `cd paper && ~/.local/bin/tectonic -X compile main.tex` (XeTeX, 24 pp). Page PNGs for inspection:
  `pdftoppm -r 120 -png main.pdf <scratch>/p`.
- Reviewer briefs/reports live in `notes/`; `MANUSCRIPT_AUDIT.md` still describes the hardening pass only
  (round-4 section not yet written).
- Nothing was deleted; checkpoints (61 GB) and corpora (~16 GB) untouched; `/home/moloch/ouro_project` untouched.
